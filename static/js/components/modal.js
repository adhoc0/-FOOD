document.addEventListener('click', (event) => { const opener = event.target.closest('[data-modal-open]'); if (opener) { const modal = document.getElementById(opener.dataset.modalOpen); modal?.removeAttribute('hidden'); modal?.querySelector('[data-modal-close]')?.focus(); } if (event.target.closest('[data-modal-close]')) event.target.closest('[role="dialog"]')?.setAttribute('hidden', ''); });
